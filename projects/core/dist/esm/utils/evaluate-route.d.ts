export declare const evaluateRoute: (browserPath: string, routePath: string, exact?: boolean) => {
    shouldActivate: boolean;
    shouldDeactivate: boolean;
    routeParams: Record<string, string>;
};
